server:
  port: 9000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jdemodb?serverTimezone=GMT
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver



mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  #type-aliases-package: com.example.entity
#showSql
logging:
  level:
    com:
      example:
        demo : debug
